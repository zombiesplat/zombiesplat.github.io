---
/**
 * Forgot Password Page
 *
 * @description
 * Password reset request page. Users enter their email to receive
 * a password reset link. Uses demo mode by default.
 */
import MarketingLayout from '@/layouts/MarketingLayout.astro';
import ForgotPasswordForm from '@forms/ForgotPasswordForm.astro';
import { Icon } from 'astro-icon/components';
import { siteConfig } from '@/config';
---

<MarketingLayout
  title={`Forgot Password - ${siteConfig.name}`}
  description="Reset your password. Enter your email address and we'll send you a link to reset your password."
>
  <section class="py-section">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-md mx-auto">
        <!-- Card container -->
        <div class="bg-surface rounded-lg border border-border p-8 shadow-sm">
          <!-- Header -->
          <div class="text-center mb-8">
            <div
              class="mx-auto w-12 h-12 rounded-full bg-primary/10 flex items-center justify-center mb-4"
            >
              <Icon name="lucide:key-round" class="w-6 h-6 text-primary" />
            </div>
            <h1 class="text-2xl font-bold text-text">Forgot your password?</h1>
            <p class="mt-2 text-text-muted">No worries, we'll send you reset instructions.</p>
          </div>

          <!-- Forgot Password Form -->
          <ForgotPasswordForm />

          <!-- Back to login link -->
          <p class="mt-6 text-center">
            <a
              href="/login"
              class="inline-flex items-center gap-1 text-sm font-medium text-primary hover:text-primary-dark transition-colors"
            >
              <Icon name="lucide:arrow-left" class="w-4 h-4" />
              Back to login
            </a>
          </p>
        </div>
      </div>
    </div>
  </section>
</MarketingLayout>
